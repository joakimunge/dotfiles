-- Joakimunge's local plugins (commented out as they reference his personal directories)
-- Harpoon has been moved to harpoon.lua with GitHub repo source

local local_plugins = {
    -- These plugins are disabled as they reference Joakimunge's personal directories
    -- To use them, you would need to clone them locally or find alternatives

    --{
    --    "99",
    --    dir = "~/personal/99",
    --    config = function()
    --        -- AI agent plugin
    --    end,
    --},

    --{
    --    "cockpit",
    --    dir = "~/personal/cockpit",
    --    config = function()
    --        require("cockpit")
    --    end,
    --},
}

return local_plugins
